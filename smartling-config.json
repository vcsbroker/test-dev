{
    "locales" : [
        {
            "smartling"   : "zh-CN",
            "application" : "zh_Hans"
        },
        {
            "smartling"   : "nl-NL",
            "application" : "nl_NL"
        }
    ],
    "resourceSets": [
        {
            "pathRegex"                 : ".*\\.properties",
            "translationPathExpression" : "/translated/${originalFile.baseName}_${locale}.${originalFile.extension}",
            "type"                      : "javaProperties",
            "translationCommitMessage"  : "fullName = ${originalFile.fullName}, basename = ${originalFile.baseName}, path = ${originalFile.path}, name = ${originalFile.name}, extension = ${originalFile.extension}, locale = ${locale}"
        }
    ]
}
